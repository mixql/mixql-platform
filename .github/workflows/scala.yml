name: Scala CI
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Formatting
        run: sbt scalafmtSbtCheck scalafmtCheckAll
on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop
